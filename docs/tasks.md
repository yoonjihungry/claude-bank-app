# Tasks

> 지금/앞으로 할 작업을 적어두는 문서.
> 완료된 항목은 체크하고 "완료" 섹션으로 옮긴 뒤, 요약은 `changelog.md`에,
> 중요한 선택 이유는 `decisions.md`에 기록한다.

## 진행 중

_(없음)_

## 보류

### [ ] 푸터
- 지금 단계에서는 넣을 내용 없음 (앱 목적: 가계부 작성으로 자금 흐름 파악에 집중)
- 필요해지면(예: 버전 정보, 데이터 백업/내보내기 안내 등) 다시 논의

## 대기 중

### [ ] 로그인 기능
- [ ] 요구사항 구체화 필요 (서버 없는 구조이므로 아래 중 방향 결정 필요)
  - 로컬 전용 잠금(PIN/비밀번호, localStorage 검증)인지
  - 추후 백엔드 연동을 염두에 둔 UI만 우선 구현인지
- [ ] 방향 정해지면 `decisions.md`에 이유 기록

## 완료

### [x] 헤더 개편
- [x] 좌측 로고 유지, 우측에 로그인 버튼 추가 (버튼 UI만 — 로그인 기능은 아래 "대기 중" 유지)
- [x] 콘텐츠 폭 규칙 `360px` → `480px` 변경 (`App.tsx` CONTENT + `design-system.md` Layout 갱신)

### [x] 하단 탭 메뉴 (홈/거래/카테고리)
- [x] 화면 가장자리에 완전히 붙는 full-width 고정바로 변경 (좌우·하단 여백 제거)
- [x] 완전 사각 + 상단 보더로 정리 (rounded/shadow 제거)
- [x] `fixed` + width 100%, 본문 하단 padding·안전영역 확보로 겹침 방지

### [x] 초기 레이아웃 구성
